area 0.0.0.0 {
    # Point-to-point links between internal nodes (WireGuard/GRE tunnels)
    # Change interface names to match your WireGuard interfaces
    interface "dn42_*" {
        type ptp;
    };

    # Loopback / dummy interface as stub
    interface "dn42-dummy" {
        stub;
    };
};
